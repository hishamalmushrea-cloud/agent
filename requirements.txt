# Core runtime
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
httpx>=0.27

# Testing
pytest>=8.0

# Device-access bridge (MCP) + desktop browser-like window
mcp<2
pywebview>=5.0

# Vision (the agent's "eyes"): screen capture + OCR
Pillow>=10.0
mss>=9.0
pytesseract>=0.3.10

# Optional / Windows / web (import guarded; comment to install on the target machine)
# playwright>=1.40          # real browser control for agent tools
# pywinauto>=0.6.8          # Windows UI Automation backend (Windows only)
# pywin32>=306              # Win32 API bindings (Windows only)
# pyautogui>=0.9.54         # last-resort mouse/keyboard automation
